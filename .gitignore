.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store

# Planning notes: the didactic review, the course structure and the LPTHW gap
# list. Author's working material, in German -- deliberately not published in the
# students' repository. Kept locally.
docs/
# A database is a build artefact: 19_sql/build_db.py makes it from the CSV
# files next to it, which are the source.
19_sql/data/readings.db
