"""Exercise 05 -- A formatted reading.

The sensor reports 22.83333. Using ONE f-string, print:

    TH-04 reports 22.8 degrees.

Tag and reading are already in variables. Round to one decimal place inside the
f-string, not with round().

Expected output:

    TH-04 reports 22.8 degrees.

Hint: the format spec goes after a colon inside the curly braces. One decimal
place, as a floating point number.
"""

tag = "TH-04"
reading = 22.83333

# TODO: a single print() line
