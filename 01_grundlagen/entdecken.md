# Modul 01 — Vorhersagezettel

**Name:** ______________________  **Datum:** ____________

Füllen Sie die acht Felder aus, **bevor** Sie `uv run 01_grundlagen/entdecken.py` starten.
Eine falsche Vorhersage ist kein Fehler, sondern der Zweck der Übung: Sie zeigt Ihnen genau die
Stelle, an der Ihre Vorstellung von Ihrem Rechner abweicht.

Bringen Sie den Zettel zur Sitzung mit.

---

**Feld 1** — `print("Sensor", "Temperatur", "OK")` und dieselbe Zeile mit `sep=" | "`.
Beide Ausgaben genau, mit allen Leerzeichen:

```


```

**Feld 2** — `a = 5` · `b = a` · `a = 10` · `print(b)`. Welche Zahl?

```

```

**Feld 3** — `7 / 2`, `7 // 2`, `7 % 2`. Drei Werte:

```

```

**Feld 4** — `type(42)`, `type("42")`, `type(42.0)`:

```


```

**Feld 5** — `int(3.9)`: wird daraus 3 oder 4? Und warum?

```


```

**Feld 6** — `temperatur = 23.456`. Was geben `{temperatur}`, `{temperatur:.1f}` und
`{temperatur=}` aus?

```


```

**Feld 7** — `bool(0)`, `bool("")`, `bool("0")`, `bool("False")`. Vier Werte:

```

```

**Feld 8** — Was ist `0.1 + 0.2`? Schreiben Sie es **genau** hin, Ziffer für Ziffer.
Und: ist `0.1 + 0.2 == 0.3` wahr?

```


```

---

**Nach dem Ausführen:** In welchem Feld lagen Sie daneben? Notieren Sie in einem Satz, was Sie
stattdessen erwartet hatten — diese Sätze sind der Einstieg in die Sitzung.

```


```
