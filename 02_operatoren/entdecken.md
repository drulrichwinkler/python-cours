# Modul 02 — Vorhersagezettel

**Name:** ______________________  **Datum:** ____________

Sieben Felder, auszufüllen **vor** `uv run 02_operatoren/entdecken.py`.
Bringen Sie den Zettel zur Sitzung mit.

---

**Feld 1** — `6 / 3`, `7 // 2`, `7 % 2`, `17 % 5`. Vier Werte — achten Sie beim ersten
auf den **Typ**:

```

```

**Feld 2** — `-17 % 5`. (Wenn Sie C oder Java kennen: raten Sie zweimal.)

```

```

**Feld 3** — `2 + 3 * 4`, `2 ** 3 ** 2`, `-3 ** 2`:

```

```

**Feld 4** — `5 == 5.0`, `"a" == "A"`, `1 < 5 < 10`, `True + True`:

```

```

**Feld 5** — `0 or "leer"`, `"a" and "b"`, `None or 0 or "x"`.
Achtung: Es sind **nicht** `True`/`False`:

```

```

**Feld 6** — `status = 0b0110`. Was geben `status`, `status & 0b0010`, `status & 0b0001`
und `bool(status & 0b0100)` aus?

```


```

**Feld 7** — `a = [1, 2]`, `b = [1, 2]`. Was ist `a == b`, was `a is b`?
Und: `3 > 2 > 1` gegen `(3 > 2) > 1`?

```


```

---

**Nach dem Ausführen:** Welches Feld hat Sie überrascht? Ein Satz dazu, was Sie erwartet hatten:

```


```
