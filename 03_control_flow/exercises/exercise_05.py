"""Exercise 05 -- Skip the gaps.

The log has holes in it: a missing reading is recorded as None. Print every
reading that is present, and count the gaps.

Expected output:

    21.7
    23.1
    22.4
    3 readings, 2 gaps

Hint: `continue` skips the rest of this turn. Test for a missing value with
`is None`, not with `== None` -- see module 02.
"""

readings = [21.7, None, 23.1, None, 22.4]

# TODO
