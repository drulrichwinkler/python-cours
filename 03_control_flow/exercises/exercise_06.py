"""Exercise 06 -- Find the first implausible reading.

Print the first reading above 85. If there is none, print that instead. Do it
with `for ... else`, without a found-flag.

Run it once as it stands, then change `readings` to the commented-out list and
run it again -- both branches have to work.

Expected output:

    first implausible: 91.0

With the second list:

    all readings plausible

Hint: the `else` of a loop runs when the loop finished without `break`.
"""

readings = [21.7, 91.0, 22.4]
# readings = [21.7, 23.1, 22.4]

# TODO
