"""Helpers for the Python course.

There is almost nothing here on purpose. Predictions in the notebooks are plain
`assert` statements, and the expected output of an exercise lives in that
exercise's own docstring -- so the course needs no machinery of its own to tell
you whether you are right.

What is left is the test harness in `course.checks`, which the tests import.
You do not need to read it to do the exercises.
"""
